package jedi.kafka.integration;

//@RunWith(MockitoJUnitRunner.class)
public class BrokerAvailablitiyTest {
 /**
  @Test
  public void test_invalid_running_broker() {
   KafkaService kafkaService = new KafkaService("test-multiple-invalid-brokers.json");
   kafkaService.isBrokersAvailable();
   assertEquals(false,kafkaService.isBrokersAvailable());
  }
  
  @Test
  public void test_valid_running_broker() {
   KafkaService kafkaService = new KafkaService("test-multiple-valid-brokers.json");
   assertEquals(true,kafkaService.isBrokersAvailable());
  }*/
}
